[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "trf-sports-lab-one"
version = "0.6.0"
description = "Market-anchored football edge engine for T.R.F SPORTS LAB ONE"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
